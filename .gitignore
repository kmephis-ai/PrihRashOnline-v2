# Local/runtime credentials and deployment-specific configuration
.env
.env.*
!.env.example
.clasp.json
.clasprc.json
credentials/
service-account*.json

# DR-001 private backup material must never enter the repository.
*.prhbackup
*.prhbackup.*
*.backupkey
*.key
*.sqlite
*.sqlite3
/backups/
/backup-keys/
/restore-drills/
dr001-evidence.private.json

# Ephemeral public-safe test evidence is produced by CI and not committed.
artifacts/
node_modules/
