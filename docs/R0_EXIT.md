# R0 exit — PrihRashOnline-v2

R0 is complete only after AIENG-003 passes the normal exact-SHA delivery chain and Main Verification closes Issue #72.

At that point:

- `MASTER-G0 / Truth` is complete;
- `MASTER-G1 / Autonomous delivery + AI engineering` is complete;
- `MASTER-G2 / Recoverability` is complete;
- all R0 master gates are complete.

AI Engineering R0 chain:

1. `AIENG-001` — root repository AI contract — DONE;
2. `AIENG-002` — executable Roadmap-to-agent task protocol — DONE;
3. `AIENG-003` — read-only exact-candidate multi-AI review protocol — DONE after Main Verification.

The AIENG-003 protocol is supplementary evidence only. Reviewer agents have no writer/merge/deploy/Issue-close authority and cannot waive deterministic machine gates.

Once Issue #72 is machine-closed, Roadmap Autopilot may enter R1. The next dependency-ordered R1 item is `FIN-010` (versioned KPI Dictionary), followed by `DATA-010` and domain/repository-adapter work according to the canonical Roadmap.

R0 completion does **not** imply full-history migration, PROD/Yandex cutover, public Dashboard access, paid provider activation or authorization to rewrite public Git history. Those remain separate policy/Roadmap boundaries.
