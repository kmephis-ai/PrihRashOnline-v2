'use strict';
const fs=require('fs'),path=require('path');const{chromium}=require('playwright');const VIEWPORTS=[{name:'desktop',width:1440,height:1000,max:1800},{name:'laptop',width:1024,height:900,max:2300},{name:'mobile',width:390,height:844,max:4200}];function expect(c,m){if(!c)throw new Error(m)}
(async()=>{const root=path.join(__dirname,'..'),dir=path.join(root,'artifacts'),htmlPath=path.join(root,'DataQualityWebApp.html');fs.mkdirSync(dir,{recursive:true});const html=fs.readFileSync(htmlPath,'utf8');expect(html.includes('SYNTHETIC • READ_ONLY'),'Synthetic marker missing');expect(html.includes('WRITE NOT AUTHORIZED'),'Mutation boundary missing');expect(html.includes('IRREVERSIBLE_ACTION_AUTHORIZED'),'MIG authorization non-reuse marker missing');expect(html.includes('GOOGLE_REPOSITORY_WRITE_POLICY_REQUIRED'),'Google write boundary missing');expect(!/https?:\/\/(?:cdn\.|unpkg\.|jsdelivr\.)/i.test(html),'External CDN forbidden');const browser=await chromium.launch({headless:true});const results=[];for(const v of VIEWPORTS){const page=await browser.newPage({viewport:{width:v.width,height:v.height},deviceScaleFactor:1});const errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto(`file://${htmlPath}`,{waitUntil:'load'});await page.waitForTimeout(100);expect(!errors.length,`[${v.name}] startup ${errors.join('|')}`);const r=await page.evaluate(max=>{const root=document.documentElement,body=document.body;const clipped=Array.from(document.querySelectorAll('.card,.label,.value,.meta,.head h2,.badge,.issue,.kind,.reason,.state,.gate,.truth,.pill')).filter(e=>e.scrollWidth>e.clientWidth+3||e.scrollHeight>e.clientHeight+3).map(e=>e.textContent.trim().replace(/\s+/g,' ').slice(0,100));return{overflow:Math.max(root.scrollWidth,body.scrollWidth)-innerWidth,pageHeight:Math.max(root.scrollHeight,body.scrollHeight),max,clipped,cards:document.querySelectorAll('.summary .card').length,issues:document.querySelectorAll('.issue').length,text:document.body.textContent}},v.max);expect(r.overflow<=1,`[${v.name}] horizontal overflow ${r.overflow}`);expect(r.pageHeight<=r.max,`[${v.name}] page too tall ${r.pageHeight}`);expect(!r.clipped.length,`[${v.name}] clipped ${r.clipped.join(';')}`);expect(r.cards===4&&r.issues===4,`[${v.name}] DQ components missing`);for(const marker of ['MISSING_INVALID','EXACT_DUPLICATE','SUSPICIOUS','PROVENANCE','WRITE NOT AUTHORIZED','REVIEW_REQUIRED','NO_AUTOFIX'])expect(r.text.includes(marker),`[${v.name}] missing ${marker}`);results.push({name:v.name,overflow:r.overflow,pageHeight:r.pageHeight,cards:r.cards,issues:r.issues});await page.screenshot({path:path.join(dir,`data-quality-${v.name}.png`),fullPage:true});await page.close()}await browser.close();fs.writeFileSync(path.join(dir,'data-quality-layout.json'),JSON.stringify({schema:'PRH_DATA_QUALITY_VISUAL_EVIDENCE_V1',privacy_class:'PUBLIC_SYNTHETIC',results},null,2));console.log('data_quality_visual_test: OK',{privacy:'PUBLIC_SYNTHETIC',results})})().catch(e=>{console.error(e);process.exit(1)});
