# Reference Web Consumer

Minimal consumer-owned web product used only by `WEBREF-001` conformance.
It deliberately avoids pre-existing ADWF `SHARED_GUARDED` paths because Managed Surface v1 does not merge differing shared files.
The React dependency is a detection marker; conformance commands use only the installed Node.js runtime and do not require registry installation.
