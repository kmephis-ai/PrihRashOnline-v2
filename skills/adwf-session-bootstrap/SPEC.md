# Contract: adwf-session-bootstrap

Цель: новая AI-сессия начинает работу не из памяти чата, а из свежего provider/repository truth.

Минимальный порядок: provider exact SHA -> PR/Issue/writer scan -> canonical programme/ledger -> durable handoff facts -> workspace truth -> delta -> current work unit.

Запрещено: считать старый SHA текущим; восстанавливать hidden chain-of-thought; объявлять recommendation implemented без evidence.
